# AGENTS.md

本仓库是研究知识库。本文件规定目录分工、权威入口和执行边界；Notes 活动框架版本为 Notes Pro-First 1.2。所有 `.md` 文件按 UTF-8 读取。执行任务时，以用户最新的明确指令确定目标和授权范围，再使用相应目录的活动规则。

## 1. 目录与文件职责

- `AGENTS.md`：仓库入口、任务分流、冲突处理和知识归属。
- `Notes/`：可长期复用的正式笔记；`Notes/00-index.md` 是面向读者的知识路线图。
- `Notes/AGENTS.md`：Notes 任务的活动路由、角色分工与权限入口。
- `Notes/WRITING_GUIDE.md`：正式正文的公共写作与实质审查标准。
- `Notes/WORKING/`：Notes 任务交接、请求、报告及经授权的分析产物。
- `CANONICAL_KNOWLEDGE.md`：面向 agent 的 canonical ownership、固定记号、适用范围与必要来源关系索引。
- `Papers/`：论文原文、来源管理和论文关系；入口为 `Papers/PAPER_MANAGEMENT.md`。
- `Translations/`：全文或局部翻译；入口为 `Translations/TRANSLATION_GUIDE.md`。
- `Translations/Snapshots/<文献ID>/`：翻译使用的原文截图，按来源自身的稳定 ID 保存。

每条公共规则由其职责文件统一维护，其余入口和模板在使用位置引用。正式知识内容放在主题笔记及读者索引中；任务性材料放在 `Notes/WORKING/` 指定的任务目录中。读者正文中的解释、来源与进一步阅读链接共同服务于知识主线。

## 2. Notes 任务入口

新增笔记、整篇重写、长推导整理、论文转写、合并拆分、学习路径重组，以及教学设计或读者反馈任务，先读取 `Notes/AGENTS.md`，再进入相应路线。

错字、链接、路径、frontmatter、纯格式化和用户已提供完整文本的精确替换，可按该入口中的 `codex-only` 路线处理。执行前确认内容含义、文件边界和 canonical ownership 的授权范围。涉及理解路径、解释、证明或语义判断的成文，由 Pro 承担。

## 3. 文献与翻译任务分流

新增、去重、替换、改名或删除 paper/book，登记新版本，改变阅读状态，或登记主辅文献关系时，先读取 `Papers/PAPER_MANAGEMENT.md`。

全文翻译和局部摘译先通过文献管理入口确认来源 ID、文件及版本，再执行 `Translations/TRANSLATION_GUIDE.md`。翻译的写入条件、进度记录、截图和验收使用该指南。

译文承担来源内容的翻译。重组解释、新推导以及从文献迁移概念的成文，进入 Notes 流程，并放在经授权的知识笔记中。一个任务同时涉及翻译与笔记时，分别明确两类产物的路径、来源和验收条件。

## 4. 不同问题的权威来源

- 任务目标、范围与授权：用户最新的明确指令。
- 仓库事实：当前工作树及活动流程指定的阶段产物；远程 Pro 读取任务绑定的固定 checkpoint。
- 数学事实与出处：实际核对的论文、教材、翻译及 canonical note。
- Notes 路由、角色与流程阶段：`Notes/AGENTS.md` 及其指向的活动文件。

发生冲突时，暂停受影响步骤，记录冲突双方、对应路径或出处，以及需要确定的事项，再按相应权威入口处理。交付回执说明已完成部分、保留的现场和下一步所需决定。

阅读记录以实际取得的文件内容为依据。缺失、截断或无法访问的材料，按当前流程记录具体缺项；文件名、摘要和旧对话用于定位材料。

## 5. 角色与交接

Notes 中的教学性、数学性和语义性成文由 Pro 负责；本地材料准备、完整性验证、上下文格式规范化、应用和 Git 操作由 Codex 负责。详细权限与阶段条件统一见 `Notes/AGENTS.md`。

文献管理与翻译任务使用各自入口规定的角色和交付方式。每次交接提供实际材料、当前版本、完成条件和明确的下一步。

## 6. 知识归属

同一概念、构造或完整推导保留一个 canonical owner。规划新内容时，先检查已有主笔记，并决定本次内容的合适落点：

- 在现有主笔记中补足或重组；
- 在当前笔记建立够用的局部桥梁，再引用主笔记；
- 经授权新增前置笔记或调整文件结构；
- 同步更新经授权的 `CANONICAL_KNOWLEDGE.md` 或 `Notes/00-index.md` 条目。

局部背景的展开深度按 `Notes/WRITING_GUIDE.md` 判断。引用已有笔记时，写清采用的结论及其在当前推导中的用途。

新增、合并、拆分或迁移方案通过 Notes 路由完成必要的设计、审查与整合。删除、移动、拆分、合并、重命名正式文件，以及改变知识归属的决定，使用该入口规定的暂停与授权机制。授权确定后，再更新有关文件及接口。

## 7. 交付回执

Notes 任务使用 `Notes/PRO_WORKFLOW.md` 与 `Notes/TEMPLATES/FINAL_REPORT.md` 规定的报告和流程回执。其它任务结束或受阻时，向用户提供：

```md
### 流程回执
- `task_id`：
- 当前阶段：
- 已完成：
- 阻塞或待确认：
- 下一位执行者：
- 下一步唯一动作：
- 用户可直接回复：
```

回执以实际结果填写，包含足以继续工作的具体信息。按任务涉及的范围说明：

- 修改了哪些文件，内容为何放在这些位置；
- 前置笔记、canonical ownership 和读者索引的处理结果；
- 任务目录及临时材料的创建、保留、归档或清理情况；
- 文献来源登记、版本、阅读状态、主辅关系、翻译范围、截图与验收状态；
- 尚存的 `待核对`、`TODO：补引用` 或 `待补推导`，以及对应的下一步。

阶段条件满足后，按已有授权继续执行；命中暂停条件时，保留现场并给出所需的唯一决定或动作。
