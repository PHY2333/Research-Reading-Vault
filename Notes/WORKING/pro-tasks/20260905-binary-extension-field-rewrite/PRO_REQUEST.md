---
task_id: 20260905-binary-extension-field-rewrite
request_id: R01
request_type: rewrite
route: pro-write-review
output_mode: full-file
review_policy: fresh
binding_id: 39fa8b2c74724b65bdb878cf9dfb4cd3
target_files:
  - Notes/08-Binary Extension Field Non Clifford Module/二元扩域.md
---

# 用户目标与真实读者反馈

用户要求 Pro 重写当前整篇笔记，明确反馈：“内容像百科，主线不够明确”。

请实际连续阅读现稿，自行诊断并在内部重做教学规划，直接交付一份完整可替换的中文教材笔记。不能把旧稿性质清单换个标题再顺序罗列，也不能仅加几句过渡或在末尾补一个“主线总结”。历史任务审查通过不代表此次读者反馈已经解决。

读完应能构造并计算二元扩域，理解抽象元素和二进制坐标的区别，进而把域内算术转成二进制线性代数，并明确固定元素乘法与两个未知元素乘法的区别。请自己决定如何把这些目标组织成读者能持续跟随的一条主线；这里不预写目录。

# Reader assumptions

可以依赖：$\mathbb F_2$ 的加乘、基础线性代数中的基／坐标／线性映射／矩阵，以及一元多项式运算和带余除法，群、环、域、逆元的基本含义。

不能依赖：商环／理想、不可约商成为域的理由、有限域存在唯一性和乘法群循环性、Frobenius／Galois 理论、迹／范数／迹配对／对偶基，或 qudit／Clifford 线路理论。仓库中有其它笔记不代表读者已经学过。标准定理若不是本文需证明的关键步骤，可准确说明采用的结论及用途；不能用一段调用更多未知定理的压缩“证明”替代说明。

# 必须读取

- 本任务的 TASK.md。
- Notes/WRITING_GUIDE.md。
- Notes/OBSIDIAN_MATH.md。
- Notes/PRO_OUTPUT_PROTOCOL.md。
- Notes/08-Binary Extension Field Non Clifford Module/二元扩域.md：完整现稿。
- CANONICAL_KNOWLEDGE.md：“二元扩域”及“二进制空间、补空间与正交补”“Lifted product code”相关条目。
- Notes/00-index.md：二元扩域的既有读者路线。
- Notes/01-量子纠错基础/二进制空间性质.md：核对线性代数边界。
- Notes/07-Lifted-Product Code/Lifted product code.md：核对一般系数代数／二进制块表示边界；不展开 LP。

所有材料均使用 Browser 提示所指定 commit。旧任务请求只属历史，不是本次教学顺序或“每个性质必须单开一节”的要求。

# Ownership 与数学边界

本篇已经是唯一 owner；重写保持现有覆盖面，但各主题的呈现深度与位置由你围绕主线决定：

- 不可约多项式商构造、唯一低次代表元、算术与求逆；抽象有限域与选定多项式／基表示的区别。
- Frobenius、乘法群和子域的必要结构；到 $\mathbb F_2$ 的绝对迹和绝对范数。
- 迹配对、对偶基、坐标提取、固定元素乘法的二进制矩阵，以及未知乘法的双线性坐标。
- 进入伽罗瓦 qudit／非 Clifford 语境前的最短代数接口。

不必把每项都写成同等规模的定理节；允许简洁准确的提醒、随用随建，或真正可跳过的补充块。必须保留 canonical 已承诺的可用结论，不能用删掉大半职责来实现“主线清楚”。不扩展为 S008 论文导读、完整 qudit Pauli／Clifford 群、门分类、扩域码或蒸馏协议。

以当前 canonical note 中的数学关系与现有固定记号为基准：$s\ge1$，$f$ 次数为 $s$，$\alpha=[x]$；列坐标 $[a]_B$；任意基 $B=(\alpha_i)$ 及迹对偶基 $B^*=(\beta_i)$；绝对迹 $\operatorname{tr}$、绝对范数 $\operatorname{Nm}$；$[\gamma a]_B=M_\gamma[a]_B$。不可约与本原不同；有限域同构类型唯一不等于存在不依赖选择的坐标对应；子域次数须整除 $s$；绝对范数对非零元素恒为 $1$；一般矩阵条目为 $\operatorname{tr}(\beta_i\gamma\alpha_j)$，不能无自对偶条件省掉对偶基。

固定非零常数乘法可逆，零常数乘法不可逆；两个未知输入乘法是双线性而非拼接输入上的线性映射。非 Clifford 的表述须限于相应计算基可逆实现，不能由“不是 CNOT/SWAP 网络”未经说明跳到完整 Clifford 层级结论。引用 LP 只表明相应表示机制，不能把一般代数误当作域，也不能无条件推出反对合与转置相容。

可以继续用 $x^2+x+1$ 的 $\mathbb F_4$ 算例建立具体操作并连接后文；怎样安排由你决定。若发现来源冲突或关键数学条件无法确定，明确返回所需上下文或阻塞，不能自行补猜。必要的外部核验限可靠教材／大学讲义／原始论文，不虚构来源或页码；本次不管理文献状态。

# 写作权限与质量要求

唯一允许输出的路径是 target_files 中的既有文件，mode 为 replace。标题保留“二元扩域”。允许完整重写段落、证明、例子、章节顺序及解释深度；不能改 allowlist 外文件或创建第二个 owner。

重点落实 Notes/WRITING_GUIDE.md：每一节的进入有前文建立的问题，退出得到后文实际使用的能力；重要术语和公式在首次承重前闭合对象、条件与用途；必要推理不断链；支持性定理不抢占教学重心。选读区不能承担后文唯一必需的理由。不要把“后续会用到”作为当前引入一串概念的唯一理由。

正文不得出现用户反馈原句、请求、审查、任务、Git、文件职责或维护者语言；不得输出独立设计稿或仅列建议。使用自然中文，严格 Obsidian $ / $$ 定界。输出前从头连续复读，确认读者能回答每个转折“为什么现在做这一步”。

# 完成标准

1. 对“像百科、主线不明”的回应体现在正文的实质组织和论证连续性。
2. 达到上述读者能力，保留已有 canonical 覆盖面与准确边界，关键构造及坐标关系可实际计算。
3. 没有靠大量新前置术语、循环解释、过长无用证明、重复结论或量子应用支线支撑结构。
4. 按 Notes/PRO_OUTPUT_PROTOCOL.md 返回完整文件、正确 binding 及 END_RESPONSE，不修改 GitHub。
